export function getHeadRenderUrl(textureId: string): string {
    return `https://nmsr.nickac.dev/head/${textureId}`;
}

export function getTextureUrl(textureId: string): string {
    return `http://textures.minecraft.net/texture/${textureId}`;
}
